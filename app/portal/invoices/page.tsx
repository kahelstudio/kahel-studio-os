export const dynamic = "force-dynamic";

import { getPortalInvoices } from "@/lib/server/customer-portal-data";
const peso = (centavos: number) => new Intl.NumberFormat("en-PH", { style: "currency", currency: "PHP", maximumFractionDigits: 0 }).format(centavos / 100);
export default async function PortalInvoicesPage() { const rows = await getPortalInvoices(); return <main className="mx-auto max-w-7xl px-4 py-10 sm:px-6"><h1 className="font-display text-4xl font-semibold">Invoices</h1><p className="mt-2 text-text-secondary">Customer-safe invoice and balance details.</p><div className="mt-8 overflow-x-auto rounded-xl border border-border bg-surface"><table className="w-full min-w-[620px] text-left text-sm"><thead className="bg-surface-muted text-text-secondary"><tr><th className="p-4">Reference</th><th className="p-4">Status</th><th className="p-4">Total</th><th className="p-4">Balance</th><th className="p-4">Due</th></tr></thead><tbody>{rows.map((row) => <tr key={row.id} className="border-t border-border"><td className="p-4 font-mono">{row.reference}</td><td className="p-4 capitalize">{row.status.replaceAll("_", " ")}</td><td className="p-4">{peso(row.totalAmount)}</td><td className="p-4 font-semibold">{peso(row.totalAmount - row.paidAmount)}</td><td className="p-4">{row.dueAt ? new Date(row.dueAt).toLocaleDateString("en-PH") : "—"}</td></tr>)}</tbody></table>{!rows.length && <p className="p-8 text-text-secondary">No invoices yet.</p>}</div></main>; }

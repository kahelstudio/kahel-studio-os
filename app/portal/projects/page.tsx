export const dynamic = "force-dynamic";

import { getPortalProjects } from "@/lib/server/customer-portal-data";
export default async function PortalProjectsPage() { const rows = await getPortalProjects(); return <main className="mx-auto max-w-7xl px-4 py-10 sm:px-6"><h1 className="font-display text-4xl font-semibold">Projects</h1><p className="mt-2 text-text-secondary">Production status and approved delivery access.</p><div className="mt-8 grid gap-4 sm:grid-cols-2">{rows.length ? rows.map((row) => <article key={row.id} className="rounded-xl border border-border bg-surface p-5"><span className="font-mono text-xs text-text-muted">{row.reference}</span><h2 className="mt-2 font-display text-xl font-semibold">{row.title}</h2><p className="mt-4 text-sm capitalize text-text-secondary">{row.status.replaceAll("_", " ")}</p></article>) : <p className="rounded-xl border border-dashed border-border p-8 text-text-secondary sm:col-span-2">Projects will appear after a booking is confirmed.</p>}</div></main>; }

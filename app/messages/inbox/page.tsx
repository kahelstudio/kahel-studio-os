import { PlannedMessages } from "@/components/messages/planned-messages";

export default function InboxPage() {
  return <PlannedMessages title="Inbox" description="Inbound email is planned but is not connected to a canonical source yet." />;
}
